# Laravel Code Review Report

**Generated:** 2026-03-03 13:00:00  
**Review Style:** Balanced  
**Laravel Version:** 12.x  
**PHP Version:** 8.2+  
**Reviewed Files:** 58  
**Total Issues:** 12  

---

## Summary

| Severity       | Count |
| -------------- | ----- |
| 🔴 Critical     | 3     |
| 🟠 Warning      | 6     |
| 🟡 Optimization | 3     |
| 🔵 Code Quality | 0     |

---

## Issues by Component

### Services

#### `App\Services\StripeService`

##### Issue 1: Potential Mass Assignment in PaymentMethod Creation

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 75-83                                     |
| **Type**     | Eloquent Anti-pattern                     |
| **File**     | `app/Services/StripeService.php`          |

**Description:**
Creating PaymentMethod with dynamic data from Stripe without strict validation could potentially lead to issues.

**Code:**
```php
// Lines 75-83
return \App\Models\PaymentMethod::create([
    'user_id' => $userId,
    'stripe_payment_method_id' => $stripePaymentMethodId,
    'type' => $stripePaymentMethod->type,
    'last4' => $stripePaymentMethod->card->last4 ?? null,
    'brand' => $stripePaymentMethod->card->brand ?? null,
    // ...
]);
```

**Recommendation:**
Ensure PaymentMethod model has proper `$fillable` attributes defined and validate Stripe data before insertion.

##### Issue 2: Missing Return Type Declaration

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟡 Optimization                            |
| **Line**     | 71                                        |
| **Type**     | PHP 8.2+ Best Practice                   |
| **File**     | `app/Services/StripeService.php`          |

**Description:**
Method `createPaymentMethod` lacks return type declaration.

**Suggested Fix:**
```php
public function createPaymentMethod(string $stripePaymentMethodId, int $userId): \App\Models\PaymentMethod
```

---

#### `App\Services\InvestmentService`

##### Issue 3: Potential N+1 Query in Investment Retrieval

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 252-257                                   |
| **Type**     | Eloquent Anti-pattern                     |
| **File**     | `app/Services/InvestmentService.php`      |

**Description:**
The `getUserInvestments` method loads investments with relations but may cause N+1 queries if additional nested relations are accessed.

**Code:**
```php
// Lines 252-257
return Investment::forUser($user->id)
    ->with(['tree', 'transaction'])
    ->orderBy('created_at', 'desc')
    ->get();
```

**Recommendation:**
Consider eager loading nested relationships if they're used in views:

**Suggested Fix:**
```php
return Investment::forUser($user->id)
    ->with(['tree.fruitCrop.farm', 'tree.fruitCrop.fruitType', 'transaction'])
    ->orderBy('created_at', 'desc')
    ->get();
```

##### Issue 4: Inconsistent Exception Handling

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🔴 Critical                                |
| **Line**     | 82                                        |
| **Type**     | Error Handling                            |
| **File**     | `app/Services/InvestmentService.php`      |

**Description:**
Generic `InvalidArgumentException` is thrown as a fallback, which makes error handling less specific.

**Code:**
```php
// Line 82
throw new \InvalidArgumentException($firstError);
```

**Recommendation:**
Create a specific exception class for investment eligibility errors to maintain consistent error handling.

---

### Controllers

#### `App\Http\Controllers\InvestmentController`

##### Issue 5: Direct Model Access in Controller

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 48-49                                     |
| **Type**     | Laravel Convention Violation              |
| **File**     | `app/Http/Controllers/InvestmentController.php` |

**Description:**
Controller directly queries the Investment model with complex eager loading instead of delegating to service layer.

**Code:**
```php
// Lines 48-49
$investment = Investment::with(['tree.fruitCrop.farm', 'tree.fruitCrop.fruitType', 'tree.harvests', 'transaction', 'payouts.harvest'])
    ->findOrFail($investment);
```

**Recommendation:**
Move complex queries to the InvestmentService to maintain separation of concerns.

##### Issue 6: Inline Data Transformation in Controller

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 28-40                                     |
| **Type**     | Laravel Convention Violation              |
| **File**     | `app/Http/Controllers/InvestmentController.php` |

**Description:**
Complex data transformation is done inline in controller instead of using API Resources.

**Recommendation:**
Create `InvestmentResource` and `TreeResource` to handle data transformation.

**Suggested Fix:**
```php
return Inertia::render('Investments/Index', [
    'investments' => InvestmentResource::collection($investments),
    'total_value_cents' => $totalValue,
    'total_value_formatted' => 'Rp ' . number_format($totalValue / 100, 2),
]);
```

##### Issue 7: Missing Authorization Policy

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🔴 Critical                                |
| **Line**     | 51-53                                     |
| **Type**     | Security Vulnerability                    |
| **File**     | `app/Http/Controllers/InvestmentController.php` |

**Description:**
Manual authorization check instead of using Laravel policies. Multiple authorization checks scattered throughout the controller.

**Code:**
```php
// Lines 51-53
if ($investment->user_id !== Auth::id()) {
    abort(403);
}
```

**Recommendation:**
Create `InvestmentPolicy` and use `$this->authorize('view', $investment)` for consistent authorization.

---

### Models

#### `App\Models\User`

##### Issue 8: Encrypted Casting on Non-Existent Field

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 71                                        |
| **Type**     | Model Configuration Error                 |
| **File**     | `app/Models/User.php`                     |

**Description:**
The model casts `kyc_document_url` as encrypted, but this field is not in the `$fillable` array.

**Code:**
```php
// Line 71
'kyc_document_url' => 'encrypted',
```

**Recommendation:**
Either add `kyc_document_url` to `$fillable` or remove the cast if the field doesn't exist.

#### `App\Models\Tree`

##### Issue 9: Missing Validation for Relationship Access

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟡 Optimization                            |
| **Line**     | 110-113                                   |
| **Type**     | Defensive Programming                     |
| **File**     | `app/Models/Tree.php`                     |

**Description:**
`isInvestable()` method assumes status enum method exists without null checking.

**Recommendation:**
Add null checks for defensive programming:

**Suggested Fix:**
```php
public function isInvestable(): bool
{
    return $this->status?->isInvestable() ?? false;
}
```

---

### Database Migrations

#### Transactions Table Migration

##### Issue 10: Missing Foreign Key Constraints

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🔴 Critical                                |
| **Line**     | 21-22                                     |
| **Type**     | Database Design Issue                     |
| **File**     | `database/migrations/2026_02_28_231612_redesign_transactions_table.php` |

**Description:**
Foreign key fields `related_investment_id` and `related_payout_id` are defined without constraints.

**Code:**
```php
// Lines 21-22
$table->foreignId('related_investment_id')->nullable();
$table->foreignId('related_payout_id')->nullable();
```

**Recommendation:**
Add proper foreign key constraints:

**Suggested Fix:**
```php
$table->foreignId('related_investment_id')->nullable()->constrained('investments')->nullOnDelete();
$table->foreignId('related_payout_id')->nullable()->constrained('payouts')->nullOnDelete();
```

---

### Frontend (TypeScript/React)

#### `resources/js/Components/TreeCard.tsx`

##### Issue 11: Weak TypeScript Typing

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟠 Warning                                 |
| **Line**     | 5                                         |
| **Type**     | TypeScript Best Practice                  |
| **File**     | `resources/js/Components/TreeCard.tsx`    |

**Description:**
Using `any` type for tree prop reduces type safety benefits.

**Code:**
```tsx
// Line 5
export default function TreeCard({ tree }: { tree: any }) {
```

**Recommendation:**
Define proper interface for tree object:

**Suggested Fix:**
```tsx
interface Tree {
    id: number;
    tree_identifier: string;
    price_cents: number;
    expected_roi_percent: number;
    risk_rating: string;
    fruit_crop?: {
        variant: string;
        harvest_cycle: string;
        fruit_type?: { name: string };
        farm?: { name: string; image_url?: string };
    };
}

export default function TreeCard({ tree }: { tree: Tree }) {
```

##### Issue 12: Missing Error Handling for Image Load

| Attribute    | Value                                     |
| ------------ | ----------------------------------------- |
| **Severity** | 🟡 Optimization                            |
| **Line**     | 14-20                                     |
| **Type**     | User Experience                           |
| **File**     | `resources/js/Components/TreeCard.tsx`    |

**Description:**
No error handling for image loading failures.

**Recommendation:**
Add `onError` handler to gracefully handle broken images.

---

## Recommendations Summary

### High Priority Actions:
1. **Create InvestmentPolicy** for proper authorization instead of manual checks
2. **Add foreign key constraints** in transactions migration
3. **Fix encrypted cast** for non-existent field in User model
4. **Implement proper exception hierarchy** for investment-related errors

### Medium Priority Actions:
1. **Create API Resources** for data transformation instead of inline controller logic
2. **Move complex queries** from controllers to service layer
3. **Add proper TypeScript interfaces** instead of using `any` types
4. **Optimize eager loading** to prevent potential N+1 queries

### Code Quality Improvements:
1. **Add return type declarations** to all service methods
2. **Implement defensive programming** with null checks
3. **Add image error handling** in React components
4. **Ensure PaymentMethod model** has proper `$fillable` definition

---

## Laravel Docs References

- [Policies](https://laravel.com/docs/12.x/authorization#creating-policies)
- [API Resources](https://laravel.com/docs/12.x/eloquent-resources)
- [Eager Loading](https://laravel.com/docs/12.x/eloquent-relationships#eager-loading)
- [Foreign Key Constraints](https://laravel.com/docs/12.x/migrations#foreign-key-constraints)

---

**Review completed successfully. Focus on addressing Critical and Warning issues first for production readiness.**